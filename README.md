# Employee-Database-Deduplication-System
Employee Database Deduplication using Python, Pandas &amp; PyQt5. A data cleaning project that removes duplicate employee records, standardizes data, performs basic EDA, and provides a user-friendly GUI for loading and saving cleaned datasets.
